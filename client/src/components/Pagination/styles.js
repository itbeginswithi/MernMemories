import {makeStyles} from '@material-ui/core/styles'

export default makeStyles(() => ({
    ul: { 
        justifyContent: 'space-around'
    },
    paper: { 
        padding: '10px',
        marginTop: '10px',
       borderRadius: 4, 
    }
}))